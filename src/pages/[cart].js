import { useState } from "react";

const Cart = () => {
  const [cart] = useState([]);

  return (
    <div>
      Hello
      {/* rest of your component code */}
    </div>
  );
};

export default Cart;
